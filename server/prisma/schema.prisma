generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model test{
  id Int @id @default(autoincrement()) 
  title String
}
